fragment scalarValue on CfgScalar {
    id
    name
    type
    value
    sourceId
}

query($id: String!) {
    cfgBranch(id: $id) {
        id
        name
        scalarValues {
            ...scalarValue
        }
        complexValues {
            id
            type
            name
            values {
                ...scalarValue
            }
            sourceId
        }
    }
}
