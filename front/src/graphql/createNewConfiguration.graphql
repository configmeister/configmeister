mutation($configurationName: String, $versionName: String, $branches: [CfgBranchInput]) {
    cfgConfiguration(value: {name: $configurationName, versions: [{
        name: $versionName
        branches: $branches
    }]}) {
        id
    }
}
