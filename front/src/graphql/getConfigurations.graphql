query($filter: CfgConfigurationInputFilter) {
    cfgAllConfigurations(filter: $filter) {
        id
        name
        updatedAt
        versions {
            id
            name
            updatedAt
        }
    }
}
